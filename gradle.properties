# JDK 24+: Gradle native-platform вызывает System::load из безымянного модуля —
# без этого флага JVM пишет предупреждения и в будущем может блокировать вызов.
org.gradle.jvmargs=-Xmx1g -Dfile.encoding=UTF-8 --enable-native-access=ALL-UNNAMED
