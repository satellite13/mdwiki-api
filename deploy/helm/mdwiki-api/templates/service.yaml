apiVersion: v1
kind: Service
metadata:
  name: {{ include "mdwiki-api.fullname" . }}
  labels:
    {{- include "mdwiki-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "mdwiki-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
