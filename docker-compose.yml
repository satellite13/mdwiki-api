services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: mdwiki
      POSTGRES_USER: mdwiki
      POSTGRES_PASSWORD: mdwiki
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
